# Docker环境配置文件
# 复制此文件为 .env 并根据需要修改配置

# 基础配置
SECRET_KEY=your-super-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# 数据库配置
DATABASE_URL=sqlite:///./data/douyin_manager.db

# Redis配置 (在Docker中会自动连接到redis服务)
REDIS_URL=redis://redis:6379

# 抖音API配置
DOUYIN_CLIENT_ID=your-douyin-client-id
DOUYIN_CLIENT_SECRET=your-douyin-client-secret
DOUYIN_REDIRECT_URI=http://localhost/auth/callback
DOUYIN_API_BASE_URL=https://open.douyin.com

# AI生成配置
OPENAI_API_KEY=your-openai-api-key
STABILITY_API_KEY=your-stability-api-key

# 文件存储配置
UPLOAD_DIR=uploads
MAX_FILE_SIZE=104857600

# CORS配置 (逗号分隔的URL列表)
BACKEND_CORS_ORIGINS=http://localhost,http://localhost:80,http://localhost:8000,http://127.0.0.1,http://127.0.0.1:80,http://127.0.0.1:8000

